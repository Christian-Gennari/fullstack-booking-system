/**
 * ⚙️ APP CONFIGURATION
 * * PURPOSE:
 * The main "Rulebook" for the Express application.
 * * SCOPE:
 * - Create the Express App.
 * - Configure Middleware (JSON parser, Static Files folder).
 * - Register all Routes (API endpoints).
 * - Export the 'app' (does NOT start the server).
 * * RELATION:
 * - Imports: All files in 'src/routes/'
 * - Imported by: 'src/server.js'
 */
